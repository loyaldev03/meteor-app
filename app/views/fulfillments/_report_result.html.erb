<% if @fulfillments %>

<table class="table table-bordered table-condensed">
	<thead>
		<th>id</th>
		<th>product</th>
		<th>trancking_code</th>
		<th>status</th>
		<th>actions</th>
	</thead>
	<tbody>
		<% @fulfillments.each do |fulfillment| %>
		<tr>
			<td> <%= fulfillment.id %> </td>
			<td> <%= fulfillment.product %> </td>
			<td> <%= fulfillment.tracking_code %> </td>
			<td> <%= fulfillment.status %> </td>
			<% if fulfillment.status == 'processing' %>
        <td><%= link_to 'Mark as sent', '#', :id => "mark_as_sent", :name => fulfillment.id,
      		      :class=> "btn btn-small"%></td>
        <td>
        	<% unless Member.find(fulfillment.member_id).wrong_address %>
        		<%= select_tag "reason", options_for_select([Settings.wrong_address.undeliverable_all, Settings.wrong_address.undeliverable_sloop, Settings.wrong_address.undeliverable_kit_card]), :id => "reason_#{fulfillment.id}" %>
      			<%= link_to 'Mark as wrong address','#', :id => "mark_as_wrong_address", 
        	    :name => fulfillment.id, :class=> "btn btn-small" %>
        	<% else %>
        		<%= t('activerecord.attributes.member.undeliverable') %>
        	<% end %>
        </td>
			<% elsif Product.find_by_sku_and_club_id(fulfillment.product,@current_club).stock > 0 %>
        <td><%= link_to 'Resend','#', :id => 'resend', :name => fulfillment.id, :class=> "btn btn-small" %></td>
     	<% end %>
		</tr>
		<% end %>
	</tbody>
</table>

<% end %>