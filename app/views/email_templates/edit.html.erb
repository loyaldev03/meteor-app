<%- model_class = @et.class -%>
<h1><%=t '.title', :default => t('activerecord.attributes.email_template.title_edit') %></h1>
<%= form_for @et, :url => terms_of_membership_email_template_path(:terms_of_membership_id => @tom.id, :id => @et.id), :html => { :id => 'et_form', :class => 'form-horizontal bbq' } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>