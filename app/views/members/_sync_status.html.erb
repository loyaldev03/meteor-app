
<div class="">
  <% if @current_member.can_be_synced_to_remote? %>
    <div class='tab-toolbar'>
      <% if @current_member.lapsed? || !@current_member.api_member || @current_member.api_id.blank? %>
        <%= 
          link_to "#", 
            class: "btn btn-success disabled",
            id: "login_remotely_as_member" do %>
          <i class='icon-signin'></i>
          <%= t('buttons.login_remotely_as_member') %>
        <% end %>
      <% else %>
        <%= 
          link_to login_as_member_path, 
            id: "login_remotely_as_member",
            class: "btn btn-success", 
            target: '_blank' do %>
          <i class='icon-signin'></i>
          <%= t('buttons.login_remotely_as_member') %>
        <% end %>
      <% end %>
      <% if @current_member.lapsed? || !@current_member.api_member || @current_member.api_id.blank? %>
        <%= 
          link_to "#", 
            method: :post, 
            class: "btn btn-info disabled",
            id: "resend_welcome_email" do %>
          <i class='icon-envelope'></i>
          <%= t('buttons.resend_welcome_email') %>
        <% end %>      
      <% else %>
        <%= 
          link_to member_resend_welcome_path, 
            method: :post, 
            class: "btn btn-info",
            id: "resend_welcome_email", 
            confirm: 'Are you sure?' do %>
          <i class='icon-envelope'></i>
          <%= t('buttons.resend_welcome_email') %>
        <% end %>
      <% end %>
      <% if @current_member.lapsed? || !@current_member.api_member %>
        <%= 
          link_to "#", 
            method: :post, 
            id: "sync_to_remote",
            class: 'btn btn-warning disabled' do %>
          <i class='icon-retweet'></i>
          <%= t('buttons.sync_to_remote') %>
        <% end %>
      <% else %>
        <%= 
          link_to member_sync_path, 
            method: :post, 
            class: 'btn btn-warning', 
            id: "sync_to_remote",
            confirm: 'Are you sure?' do %>
          <i class='icon-retweet'></i>
          <%= t('buttons.sync_to_remote') %>
        <% end %>
      <% end %>
      <% if @current_member.lapsed? || !@current_member.api_member || @current_member.api_id.blank? %>
        <%= 
          link_to "#", 
            method: :post,
            id: "password_reset",
            class: "btn btn-warning disabled" do %>
          <i class='icon-key'></i>
          <%= t('buttons.password_reset') %>
        <% end %>
      <% else %>
        <%= 
          link_to member_reset_password_path, 
            method: :post, 
            class: "btn btn-warning", 
            id: "password_reset",
            confirm: 'Are you sure?' do %>
          <i class='icon-key'></i>
          <%= t('buttons.password_reset') %>
        <% end %>
      <% end %>
      <% if @current_member.lapsed? || !@current_member.api_member || @current_member.api_id.blank? %>
        <%= 
          link_to "#", 
            class: "btn btn-success disabled",
            id: "show_remote_data",
            style: 'float: right;' do %>
          <i class='icon-download-alt'></i>
          <%= t('buttons.show_remote_data') %>
        <% end %>  
      <% else %>
        <%= 
          link_to member_sync_data_path, 
            remote: true, 
            class: "btn btn-success",
            id: "show_remote_data",
            style: 'float: right;',
            data: { replace: '#sync-data' } do %>
          <i class='icon-download-alt'></i>
          <%= t('buttons.show_remote_data') %>
        <% end %>    
      <% end %>
    </div>
  <% end %>

  <table class='hidden_table_border'>
    <tr>
      <td NOWRAP><%= t('activerecord.attributes.member.api_id') %>:</td>
      <td>
        <span class='toggler-content default active' id="span_api_id">
          <% if @current_member.api_id.present? %>
            <%= @current_member.api_id %>
          <% else %>
            <em>none</em>
          <% end %>
          <span class='label label-<%=t @current_member.sync_status, scope: 'activerecord.attributes.member.sync_status_priorities' -%>' id="span_mi_sync_status">
            <%=t @current_member.get_sync_status, scope: 'activerecord.attributes.member.sync_status_captions' -%>
          </span>
        </span>
        <% if @current_member.can_be_synced_to_remote? %>
          <span class='toggler-content'>
            <%= form_for @current_member, url: member_update_sync_path, html: { class: 'form-inline', style: 'display: inline;' } do |f| %>
              <%= f.text_field :api_id, :class => 'text_field' %>
              <%= f.submit 'Update', class: 'btn-danger', confirm: 'Are you sure?' %>
            <% end %>
          </span>
          &nbsp;
          <%= link_to '#', :id=>"edit_api_id" ,class: 'btn btn-success toggler', data: { target: '.toggler-content' } do %>
            <i class='icon-pencil'></i>
            <span class='toggler-content default active'><%= t('buttons.edit') %></span>
            <span class='toggler-content'><%= t('buttons.stop_editing') %></span>
          <% end %>
          <% if @current_member.api_id.present? %>
            <%= link_to member_update_sync_path(member: { api_id: '' }), method: :put, confirm: 'Are you sure?', class: 'btn btn-warning' do %>
              <i class='icon-remove'></i>
              <%= t('buttons.unset') %>
            <% end %>
          <% end %>
        <% end %>
      </td>
    </tr>
    <tr>
      <td><%= t('activerecord.attributes.member.last_synced_at') %>:</td>
      <td colspan='999' id="td_mi_last_synced_at">
        <%= 
          @current_member.last_synced_at && 
          l(@current_member.last_synced_at, format: :dashed) 
        %>
      </td>
    </tr>
    <tr>
      <td><%= t('activerecord.attributes.member.last_sync_error') %>:</td>
      <td colspan='999' id="td_mi_last_sync_error_at">
        <% if @current_member.last_sync_error_at %>
          <code><%= @current_member.last_sync_error.html_safe %></code>
          on
          <em>
            <%=l @current_member.last_sync_error_at, format: :dashed -%>
          </em>
        <% else %>
          <em>none</em>
        <% end %>
      </td>
    </tr>
    <tr>
      <td><%= t('activerecord.attributes.member.autologin_url') %>:</td>
      <td colspan='999' id="td_autologin_url">
        <% if @current_member.autologin_url %>
          <%= link_to @current_member.full_autologin_url.to_s, target: '_blank' -%>
        <% else %>
          <em>none</em>
        <% end %>
      </td>
    </tr>
  </table>
</div>

<div id='sync-data'></div>

<script>
  $(document).ready(sync_status_member_functions(<%= Operation.datatable_columns.size %>));
</script>
