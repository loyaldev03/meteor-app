<% @current_member = Member.find_by_visible_id_and_club_id(params[:member_prefix], @current_club.id) if @current_member.nil? %>

<%= form_for(member_set_undeliverable_path(@current_partner.prefix,@current_club.name,@current_member), 
    :html => {:id => "set_undeliverable", :name => @current_member.visible_id} ) do %>
        <%= text_field_tag "reason",nil ,:class => 'text_field input-medium', :placeholder => t('reason') %>
        <%= submit_tag t('buttons.set_wrong_address'), :class => 'btn btn-primary', :confirm => t('are_you_sure') %> 
<% end %>