<%- model_class = @tom.class -%>
<h1><%=t '.title', :default => t('activerecord.attributes.terms_of_membership.wizard.title_edit') %></h1>
<%= form_for @tom, :url => terms_of_membership_url(:partner_prefix => @current_partner.prefix), :html => { :id => 'tom_wizard_form', :class => 'form-horizontal bbq' } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>
