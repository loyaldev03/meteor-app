<table id="memberships_table" class="table table-bordered table-condensed dataTable" data-source="<%= memberships_url(format: "json") %>">
  <thead>
    <tr>
      <th id="th_id"> <%= t('activerecord.attributes.membership.id') %> </th>
      <th> <%= t('activerecord.attributes.membership.status') %> </th>
      <th> <%= t('activerecord.attributes.membership.tom') %> </th>
      <th> <%= t('activerecord.attributes.membership.join_date') %> </th>
      <th> <%= t('activerecord.attributes.membership.cancel_date') %> </th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>

<script>
  $(document).ready(memberships_user_functions(<%= Membership.datatable_columns.size %>));
</script>