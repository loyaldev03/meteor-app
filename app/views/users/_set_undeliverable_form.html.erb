<% @current_user = User.find_by(id: params[:user_prefix], club_id: @current_club.id) if @current_user.nil? %>

<%= form_for(user_set_undeliverable_path(@current_partner.prefix,@current_club.name,@current_user), 
    :html => {:id => "set_undeliverable", :name => @current_user.id} ) do %>
        <%= text_field_tag "reason",nil ,:class => 'text_field input-medium', :placeholder => t('reason') %>
        <%= submit_tag t('buttons.set_wrong_address'), :class => 'btn btn-primary', :confirm => t('are_you_sure') %> 
<% end %>