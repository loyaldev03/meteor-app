<div class='row-fluid'>
  <div class='span12'>
		<h2>New <%= t('activerecord.model.campaign') %></h2>
		<hr />
	</div>
</div>
<%= form_for @campaign, :html => { :class => 'form-horizontal', :id => 'new_campaign' } do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>